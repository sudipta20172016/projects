(function() {

    angular.module("musicApp")
        .controller("initCtrl", ["$rootScope", "$scope", "$http", "$location", "$timeout", "$sce", function ($rootScope, $scope, $http, $location, $timeout,$sce) {
            



        }]);
})();